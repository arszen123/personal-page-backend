# PersonalPage project backend
## RUN

First need to create the ```.env``` files based on ```.env.sample``` files.

Run ```docker-compose up```.
After the containers started, in the ```migrations``` directory run: ```npm install && npm run mm```
